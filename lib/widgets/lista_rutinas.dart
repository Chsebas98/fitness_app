import 'package:flutter/material.dart';

class ListaRutinas extends StatelessWidget {
  const ListaRutinas({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('ListaRutinas');
  }
}
